# Installed on the machine, not versioned
ComfyUI/

# Keys y logs — NUNCA al repo
config/keys.json
config/presets.local.json
logs/
*.log

# Local working notes (never in the public repo)
MIS-PASOS.md
__pycache__/
